<%= render 'layouts/banner-index' %>

<div class="projects">
  <div class="container">
    <div class="row">
      <% @ngos.each do |ngo| %>
          <div class="card-project col-xs-10 col-xs-offset-1">
            <%= render 'layouts/card-project', ngo: ngo  %>
          </div>
      <% end %>
    </div>
  </div>
</div>

