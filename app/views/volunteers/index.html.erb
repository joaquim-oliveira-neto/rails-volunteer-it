<div class="wrapper">
  <div class="projects">
    <div class="container">
      <div class="row">
        <p>Meus projetos</p>
        <hr>
        <% @projects.each do |project| %>
              <%= render 'layouts/card-project', project: project  %>
        <% end %>
      </div>
    </div>
  </div>
</div>
